namespace crpc.sample

service Echo
{
    i32 echo(i32 p);
    void do_void_test(i32 p);
    null do_null_test(i32 p);
    condcast do_condcast_test(i32 p);
    uncondcast do_uncondcast_test(i32 p);
}

service Gateway
{
    void recv(string msg);
}
